" Set line numbers by default
set number

" Enable auto-indentation
set autoindent

" Enable syntax highlighting
syntax enable

" Use Dracula colorscheme from ~/.vim/pack/themes/start
colorscheme dracula
